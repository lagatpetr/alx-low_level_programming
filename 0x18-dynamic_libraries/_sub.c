#include <stdio.h>
#include "list.h"
/**
 * sub - subtracts two ints
 * @a: first int
 * @b: second int
 * Return: subtraction of the two ints
 */
int sub(int a, int b)
{
	int s;

	s = a - b;
	return (s);
}
