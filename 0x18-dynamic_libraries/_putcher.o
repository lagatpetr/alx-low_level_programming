#include "main.h"

/**
 * _puts - prints a string on stdout
 * @s: the string to be printed
 *
 * Return: the printed string
 */
void _puts(char *s)
{
	int a;

	for (a = 0; s[a] != 0; a++)
	{
		_putchar(s[a]);
	}
	 _putchar('\n');

}
