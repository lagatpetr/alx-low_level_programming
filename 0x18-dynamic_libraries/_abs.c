#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: is the integer whose value is computed
 *
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
