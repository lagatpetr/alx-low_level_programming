#include "list.h"
#include <stdio.h>
/**
 * mul - multiplies two ints
 * @a: first int
 * @b: second int
 * Return: multipltication value of the two
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
